<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">
  </head>
  <body id="page-top">
    <% if Helper.is_logged_in?(@session) %>
      <p> Welcome <%= Helper.current_user.name %> </p>
      <p> <%= Helper.current_user.balance %> </p>
    <% else %>
      <p>You Must <a href="/">Log In</a> to View Your Balance
    <% end %>


    <a href="/logout">Log Out</a>

  </body>
</html>
